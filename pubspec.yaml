name: layn_app
description: Layn — видеохостинг и шортсы
publish_to: 'none'

version: 1.5.0+6

environment:
  sdk: ^3.4.0

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  provider: ^6.1.1
  cached_network_image: ^3.4.0
  video_player: ^2.8.1
  chewie: ^1.7.5
  shared_preferences: ^2.2.2
  package_info_plus: ^8.0.0
  path_provider: ^2.1.0
  url_launcher: ^6.2.0
  wakelock_plus: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/version.json

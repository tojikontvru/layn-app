name: layn_app
description: "Layn — mobile app for layn.su"
publish_to: 'none'
version: 1.0.0+15

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  cached_network_image: ^3.3.0
  video_player: ^2.8.0
  chewie: ^1.8.0
  shared_preferences: ^2.2.0
  provider: ^6.1.0
  html: ^0.15.4
  intl: ^0.19.0
  path_provider: ^2.1.0
  share_plus: ^10.0.0
  wakelock_plus: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/logo.png
    - assets/images/app_icon.png
